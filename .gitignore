# Large data files (CSV and JSON) - users download their own
*.csv
*.json

# Output files - generated by scripts
scripts/grant_classifier/output/

# Python
__pycache__/
*.py[cod]
.env

# OS
.DS_Store

# Keep Excel validation files (they're templates!)
!*.xlsx
.git.backup

# Personal workflow files — live only on your machine, not in GitHub.
# Paths inside these are hard-coded to colbey-derwin's Desktop, so they shouldn't
# be committed anywhere shared.
sync_to_sarah.sh
WORKFLOW.md
.claude/

# Local-only scripts (retries, one-off diagnostics, ad-hoc work).
# Not committed to personal repo, not synced to Sarah's workflow repo.
local/

# Patent-translation pipeline (private academic work, runs on Lens Trial
# non-commercial license) — not for the public climate-biotech repo.
scripts/patent_translation/

# Biomining-side analysis — local-only, not for climate-biotech repo.
scripts/patent_translation/biomining/

# Ad-hoc infrastructure-subtype reclassification — local-only until subtypes
# are confirmed and merged into the main pipeline.
scripts/grant_classifier/adhoc_infrastructure_reclassify/

# Infrastructure-side patent-translation pipeline — local-only, parallel to
# scripts/patent_translation/biomining/
scripts/patent_translation/infrastructure/
